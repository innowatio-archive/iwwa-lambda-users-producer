language: node_js

node_js:
  - iojs

script:
  - npm test
  - npm run coveralls
  - npm run lint

env:
  - CXX=g++-4.8

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.8
